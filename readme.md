Repo responsável pelo boilerplate do redis com configuração de filas e cache

# Instalar o redis pelo docker compsose

```
    docker rm -f redis
    docker compose build --no-cache
    docker compose up -d 
```
